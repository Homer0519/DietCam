# 复制成 android/keystore.properties 后填上真实口令（该文件已被 .gitignore 忽略）。
# 密钥库本身（android/keystore/dietcam-release.jks）继续留在仓库里 ——
# 覆盖升级依赖它，换掉它会让所有用户必须卸载重装。
storeFile=keystore/dietcam-release.jks
storePassword=在这里填密钥库口令
keyAlias=dietcam
keyPassword=在这里填密钥口令

# DietCam 仓库（astrbot_plugin_diet 插件 + Android 客户端）
# 这个仓库自成一体：插件、App、构建工具都在这里。

# ---------------------------------------------------------------- Python
__pycache__/
*.py[cod]
.venv/
venv/

# ---------------------------------------------------------------- 编辑器 / 系统
.vscode/
.idea/
.DS_Store
*.iml

# ---------------------------------------------------------------- Android 构建产物
*.apk
*.aab
captures/
android/.gradle/
android/build/
android/app/build/
android/local.properties

# 本机构建工具链（Android SDK + Gradle，约 1.9 GB，不要提交）
.toolchain/

# ---------------------------------------------------------------- 本机产物与临时数据
# 插件包（由 tools/package_plugin.py 生成）
dist/*.zip
dist/*.bundle
# 自测 / 演示用的临时数据
.demo_data/
.selftest_data/
.ziptest/

# 签名材料一律不进版本库。本机放 android/keystore/ 下，
# CI 从 GitHub Secrets（KEYSTORE_BASE64 / KEYSTORE_PROPERTIES）还原。
# 丢了密钥库 = 所有用户必须卸载重装，务必在仓库之外另存一份。
android/keystore/*.jks
android/keystore/*.keystore
android/keystore.properties
